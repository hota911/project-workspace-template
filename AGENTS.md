# エージェントの作業ルール

## 作業の開始と終了

- README と TODO.md、対象プロジェクトの文書、直近の日別ログを読んでから作業する。
- 開始時に `logs/YYYY-MM-DD.md` へ予定を短く記録する。日付は原則として日本時間とし、別の運用が合意されていれば従う。
- 日別ログがなければ `logs/_template.md` をコピーする。同じ日の作業は同じファイルに記録する。
- 終了時に予定を実施結果へ更新し、TODO の完了、期限、確認待ちを見直す。本人にしか判断できない項目は勝手に完了にしない。
- 次回に必要な事実や判断を会話だけに残さず、該当プロジェクトの文書へ記録する。

## 文書と TODO

- TODO はルートの TODO.md に集約する。プロジェクト別に整理し、必要な期限と確認相手を書く。
- 共通の関係者や知識などの Stock 型の文書は `docs/`、プロジェクト固有の文書は `projects/<name>/` に置く。各 README から関連文書へリンクする。
- プロジェクトの README は現状、ルートの `logs/YYYY-MM-DD.md` は日付別の経緯を記録する。実行タスクは TODO.md へリンクする。
- 大きな作業は `logs/YYYY-MM-DD-<作業名>.md` に独立させる。日付は開始日とし、複数日にまたがっても同じファイルを更新する。各日の日別ログには進捗の要約とリンクを残す。単発作業も同じ方法で扱う。
- 現状を説明する文書は、初見の読者が理解できるよう全体を見直す。同じ説明を末尾へ足し続けない。
- 原文を要約、整理するときは原文を別の節やファイルへ保存する。各項目を残したか、未確認か、意図して省略したかを説明する。
- 事実、本人の判断、見積もり、未確認事項を区別する。根拠の資料と確認日を示す。
- 氏名、製品名、ツール名を曖昧な表記から推測して確定しない。
- 提案中の条件を合意済みとして扱わない。追加依頼は範囲、期間、稼働への影響を記録する。
- 短く具体的に書く。チャットはですます調、内部文書はである調とし、共有文書は相手に合わせる。日付を明示し、造語と説明の重複を避ける。

## 稼働時間

- 稼働時間は `logs/time/YYYY-MM.csv` に月ごとに記録する。列と記入方法は [稼働時間の記録方法](logs/time/README.md) に従う。Markdown には重複して記録しない。
- 当月の CSV がなければ `logs/time/_template.csv` をコピーする。開始時に行を追加し、終了時に同じ行を更新する。再開した作業は行を分ける。
- 時刻または稼働時間は本人の申告や確認済み資料を使う。作業時間だけ分かる場合は稼働分だけ記入し、時刻は空欄とする。申告済みの値は再確認しない。
- 請求情報が必要な場合のみ請求対象の列を追加し、本人に確認する。
- CSV の不明な値は空欄とし、0 と区別する。エージェントの実行時間から本人の稼働時間を推定しない。
- 確認済みの開始から終了までを原則100％の稼働とし、短い待ち時間を勝手に差し引かない。休憩や並行作業の調整は本人の申告を反映する。
- 日をまたぐ作業は各日へ分け、同じ時間を二重に記録しない。
- 大きな作業を独立したログに記録した場合も、稼働時間は月別 CSV に集約する。集計は確認済みの稼働分を対象とし、未確認の件数を明記する。CSV に合計行を追加しない。
- 請求用の集計への転記は onboarding.md の合意した方法に従う。未確認なら自動で転記しない。

## 情報管理

- 利用するツールと入力できる情報の範囲を onboarding.md で確認する。共同作業では関係者との合意も確認する。未確認の機微情報を外部サービスへ送らない。
- 候補者資料、人物評価、給与データ、音声ファイルは、このセッションで本人の明示的な確認がない限り端末外へ送らない。private リポジトリも例外としない。
- 認証情報を文書へ記入しない。認証情報や外部共有できない資料は Git の対象外とする `local/` に置く。
- 社外向けの資料へ本人の受注判断や人物評価を混ぜない。
- 個人の会議資料の保管方法を共有文書に記載しない。根拠は会議の日付と名称で示す。
- 外部へのメッセージ送信は明示的な依頼を受けてから行う。
- ファイルを移し替えるときはコピーして検証し、元の削除は対象を示して確認を得てから行う。
- ホストへのソフトウェアのインストールは事前に確認する。

## 検証と引き継ぎ

- 変更した文書のリンク、記録先、TODO の状態を確認する。検証したことと未確認のことを分けて報告する。
- ほかの作業者の変更を上書きしない。Git を利用する場合はコミット前に差分を読み、機微情報が含まれないことを確認する。
- 指示の本文はこのファイルで更新する。CLAUDE.md は `@AGENTS.md` の読み込みを維持する。
- プロジェクト固有の AGENTS.md を追加する場合は、同じディレクトリに `@AGENTS.md` を記載した CLAUDE.md も置く。
